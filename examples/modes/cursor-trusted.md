# Cursor trusted mode

Cursor can use the mode-aware helper for project-scoped notes.

```bash
agent-kernel-mode set trusted
agent-kernel-agent-write --from cursor --type project-note --scope project --level note --text "<memory>"
```
