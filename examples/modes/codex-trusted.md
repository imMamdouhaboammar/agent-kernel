# Codex trusted mode

Codex can auto-write low-risk project memory through the mode-aware helper.

```bash
agent-kernel-mode set trusted
agent-kernel-agent-write --from codex --type project-note --scope project --level note --text "<memory>"
```
