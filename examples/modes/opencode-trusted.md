# OpenCode trusted mode

OpenCode can auto-write low-risk project notes through the mode-aware helper.

```bash
agent-kernel-mode set trusted
agent-kernel-agent-write --from opencode --type project-note --scope project --level note --text "<memory>"
```
