# Antigravity trusted mode

Antigravity can auto-write low-risk project notes through the mode-aware helper.

```bash
agent-kernel-mode set trusted
agent-kernel-agent-write --from antigravity --type project-note --scope project --level note --text "<memory>"
```
