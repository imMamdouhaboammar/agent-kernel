# Antigravity approval mode

Antigravity should create pending proposals only.

```bash
agent-kernel-mode set approval
agent-kernel-agent-write --from antigravity --reason "User asked Antigravity to remember this" --text "<memory>"
```
