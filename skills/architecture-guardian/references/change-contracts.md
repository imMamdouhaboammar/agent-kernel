# Change contracts

A change contract is a reviewable statement of intended scope. It names the task owner, allowed and forbidden file patterns, expected files, approved new dependencies, required tests, and notes.

Contracts are not architecture documents. They are short-lived execution boundaries. Close them after the change. Expand a contract through review rather than editing outside it.
