# Exceptions

Exceptions are explicit, scoped, owned, and expiring. They can match a rule, a finding fingerprint, and file patterns.

Every exception requires a reason and expiry date. Revoke exceptions when the migration or legacy constraint ends. Broad permanent ignore rules are not valid substitutes for architecture decisions.
