# Helper package requirements

## Required helper bins

```text
agent-kernel-safe-link
agent-kernel-safe-git-hook
```

## Why they stay

They support transitional scripts while public commands become the preferred production path.

## Safety rule

Do not remove helper bins until a documented migration window is complete.
