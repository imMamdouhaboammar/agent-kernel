# Mode install test plan

## Cases

1. install approval mode
2. install trusted mode
3. install bypass mode
4. verify `agent-kernel-mode show`
5. verify helper binaries exist on PATH

## Pass condition

The selected install mode is reflected in config and helper behavior.
