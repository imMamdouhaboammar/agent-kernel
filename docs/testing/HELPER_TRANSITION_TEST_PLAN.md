# Helper transition test plan

## Cases

1. `agent-kernel-safe-link` still works
2. `agent-kernel-safe-git-hook` still works
3. `agent-kernel link` uses equivalent behavior
4. `agent-kernel git-hook install` uses equivalent behavior
5. docs mark helpers as transitional, not primary

## Pass condition

Users can migrate to public commands without losing access to helper binaries.
