# ak alias migration

`ak` remains a safe alias for `agent-kernel`.

## Preferred usage

```bash
ak link .
ak git-hook install .
```

## Equivalent usage

```bash
agent-kernel link .
agent-kernel git-hook install .
```

## Safety rule

The alias must route through the same wrapper as the full command.
