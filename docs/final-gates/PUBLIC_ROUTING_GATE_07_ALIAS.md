# Gate 07: Alias safety

`ak` must route through the same public wrapper as `agent-kernel`.

## Check

```bash
ak link .
ak git-hook install .
```

## Pass condition

Alias behavior matches full command behavior.
