# Gate 08: Helper continuity

Transitional helper binaries must remain available while public routing ships.

## Check

```bash
agent-kernel-safe-link --help
agent-kernel-safe-git-hook --help
```

## Pass condition

Helper commands still resolve after install.
