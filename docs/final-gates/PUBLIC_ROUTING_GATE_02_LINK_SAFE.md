# Gate 02: Public link safety

`agent-kernel link` must preserve existing project instruction files.

## Check

Create a project with an existing `AGENTS.md`, run `agent-kernel link .`, and confirm local content remains.

## Pass condition

The Agent Kernel block is added without deleting user-owned content.
