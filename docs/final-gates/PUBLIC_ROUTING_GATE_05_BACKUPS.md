# Gate 05: Backups

Public safe commands must create backups before modifying existing user-owned files.

## Check

Run routed commands on files that already exist.

## Pass condition

`.agent-kernel-backups/` contains backups for changed files.
