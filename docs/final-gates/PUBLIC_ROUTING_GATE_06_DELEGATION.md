# Gate 06: Delegation

The wrapper must delegate non-routed commands to the runtime CLI.

## Check

```bash
agent-kernel --version
agent-kernel doctor
agent-kernel status
```

## Pass condition

Non-routed commands keep their existing runtime behavior.
