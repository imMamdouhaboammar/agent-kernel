# OpenCode mode mapping

## approval

OpenCode writes pending proposals.

## trusted

OpenCode can directly write low-risk project-scoped memory.

## bypass

OpenCode can write approved memory directly only when bypass is explicitly selected.

## Safety rule

OpenCode should treat `AGENTS.md` mode instructions as authoritative.
