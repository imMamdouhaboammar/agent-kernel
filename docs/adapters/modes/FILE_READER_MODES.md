# File-reader mode mapping

File-reader agents cannot write memory directly.

## approval

Ask the user to run a proposal command.

## trusted

Ask the user to run the mode-aware helper.

## bypass

Ask the user to run the mode-aware helper only if bypass was explicitly selected.

## Safety rule

Do not claim memory was saved when the agent cannot run commands.
