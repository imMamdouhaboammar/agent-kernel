# Reporting

Reports are written to `.agent-kernel/architecture/reports/latest.json`. Status is `passed`, `review`, or `failed`. Only new unsuppressed findings can block. Pre-existing findings, suppressed findings, resolved fingerprints, and architecture diffs remain visible for audit.
