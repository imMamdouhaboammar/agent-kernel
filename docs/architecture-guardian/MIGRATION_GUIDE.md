# Adoption guide

Start in `review` mode. Discover the project and inspect the report. Create a baseline only after reviewing existing findings. Add layers incrementally, beginning with boundaries the team already agrees on. Enable contract-required writes for high-risk repositories after the team has practiced the workflow. Move to `strict` mode in CI only when the false-positive rate is acceptable.
