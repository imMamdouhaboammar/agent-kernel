# Bypass release requirements

Bypass mode must not ship as a silent default.

## Requirements

- explicit command required
- warning documented
- tests prove direct write behavior
- rollback to approval documented
- future audit path specified

## Safety rule

Bypass is a user-selected speed mode, not the normal governance model.
