# Public commands runbook

## Daily commands

```bash
agent-kernel link .
agent-kernel git-hook install .
agent-kernel doctor
```

## Expected behavior

Project files and hooks are preserved by default.

## Safety rule

Use public commands in documentation and examples instead of transitional helpers.
