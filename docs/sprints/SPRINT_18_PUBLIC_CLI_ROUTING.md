# Sprint 18: Public CLI routing

## Goal

Make safe linking and safe hook installation the default public command behavior.

## Scope

- wrapper public bin
- routed `link`
- routed `git-hook install`
- public routing smoke tests
- transition docs

## Done when

Users can run `agent-kernel link` and `agent-kernel git-hook install` safely.
