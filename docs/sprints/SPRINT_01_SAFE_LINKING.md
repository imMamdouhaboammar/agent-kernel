# Sprint 01: Safe linking

## Goal

Make project linking safe by default.

## Scope

- preserve existing `AGENTS.md`
- preserve existing `GEMINI.md`
- preserve Cursor rules
- preserve `.agents` files
- write backups before mutation

## Done when

`agent-kernel link` uses the same merge strategy as `agent-kernel-safe-link`.
