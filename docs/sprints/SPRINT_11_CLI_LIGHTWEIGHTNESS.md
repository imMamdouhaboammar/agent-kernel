# Sprint 11: CLI lightweightness

## Goal

Keep Agent Kernel easy to install and light to run.

## Scope

- avoid heavy runtime dependencies
- keep JSON-first storage
- keep hosted backend optional
- keep helper binaries small
- keep package contents intentional

## Done when

The package remains installable as a lightweight native CLI while supporting shared memory across agents.
