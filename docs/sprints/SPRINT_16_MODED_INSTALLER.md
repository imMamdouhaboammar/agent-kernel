# Sprint 16: Moded installer

## Goal

Make install mode selection part of setup.

## Scope

- prompt user for mode
- default to approval
- warn before bypass
- persist mode in config
- verify with `agent-kernel-mode show`

## Done when

Install commands can produce a known, tested mode state.
