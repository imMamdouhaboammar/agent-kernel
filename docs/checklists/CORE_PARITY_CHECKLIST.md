# Core parity checklist

- [ ] core `link` preserves files
- [ ] core `git-hook install` preserves hooks
- [ ] wrapper and core behavior match
- [ ] helper and core behavior match
- [ ] tests prove parity

## Release blocker

Do not remove wrapper routing before parity is proven.
