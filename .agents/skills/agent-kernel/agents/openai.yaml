interface:
  display_name: "Agent Kernel"
  short_description: "Repo-specific patterns and workflows for agent-kernel"
  default_prompt: "Use the agent-kernel repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true